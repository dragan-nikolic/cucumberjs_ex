/*
cucumber.js

The cucumber.js defines profiles and project variables.
*/

module.exports = {
  // environments
  dev: "--tags '@dev or @all'",
  stage: "--tags '@stage or @all'",
  prod: "--tags '@prod or @all'",
}
